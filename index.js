function writeCards(george, birthday) {
    let cardsArray = {};
    for (let 1=0; 1 < george,length; 1++){
        cardsArray.push(`Thank you, ${george{1}},for the wonderful ${birthday} gift`);}
        return cardsArray;
    }

function countDown(number) {
    while (number >= 0) {
        console.log(number);
        number==;
    }
}
